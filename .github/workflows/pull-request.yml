name: 'Pull Request'
on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - .gitignore
      - README.md
      - CODE_OF_CONDUCT.md
      - LICENSE

jobs:
  pull-request:

    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@3.6.0

        with:
          fetch-depth: 0  # for posts's lastmod

      - name: Setup Ruby
        uses: ruby/setup-ruby@1.221.0
        with:
          ruby-version: 3.2.2
          bundler-cache: true

      - name: Test Site
        run: bash tools/test.sh
        env:
          JEKYLL_ENV: production

